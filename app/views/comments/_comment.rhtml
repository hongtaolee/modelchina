    <td>[<%=comment.id %>]<%=comment.status %></td>  
    <td><%=comment.model_id %></td>
    <td><%=comment.user_id %></td> 
    <td><%=simple_format(comment.text)%></td>
    <td><%= link_to_remote '删除', :url => { :action => 'destroy', :id => comment.id }, :confirm => '确定删除' %>
     | <%= link_to_remote '删除该用户全部', :url => { :action => 'destroy_user', :user_id => comment.user_id }, :confirm => '确定删除' %>
     | <%= link_to_remote '审核通过', :url => { :action => 'proof', :id => comment.id } %></td>